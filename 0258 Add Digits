class Solution(object):
    def addDigits(self, num):
        while len(str(num)) > 1:
            temp = 0
            for i in str(num):
                temp += int(i)
            num = temp
        return num
